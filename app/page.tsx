// This root page component is necessary for the App Router structure,
// but Next.js's i18n routing should handle the redirect to the default locale.
// We don't need to explicitly redirect here.
export default function RootPage() {
  return null; // Or a loading indicator if preferred
}
